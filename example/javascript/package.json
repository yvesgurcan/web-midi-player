{
    "name": "web-midi-player-javascript-example",
    "description": "Web Midi Player JavaScript example.",
    "scripts": {
        "start": "serve -l 8081 --symlinks",
        "lint": "prettier --write **/*.js",
        "link:make": "npm run link:delete && ln -s ../../../index.js ./src/web-midi-player.js",
        "link:delete": "rm ./src/web-midi-player.js"
    },
    "devDependencies": {
        "prettier": "^1.19.1",
        "serve": "^11.3.0"
    }
}
